# Tasks: Format lại toàn bộ Rule theo Phòng ban & Chức vụ

## 1. Prisma Schema và Migration

- [ ] 1.1 Thêm bảng `Resource` (`@@schema("auth")`, fields: code PK, label, group, sortOrder, isActive) và seed ≥60 business resources từ scan `ROUTE_MAP` (82 keys, loại trừ auditLog/docs/myHistory). Thêm enum `RuleScope` (`GLOBAL | DEPARTMENT | SUB_DEPARTMENT` — `POSITION_LEVEL` để phase 2); thêm bảng `Rule` (`@@schema("auth")`, fields: id cuid, resourceCode FK Resource.code, action PermissionAction, scope RuleScope, departmentId? FK Department.id nullable, subDepartmentId? FK SubDepartment.id nullable, positionId? FK Position.id nullable, role? UserRole nullable, allow Boolean, isActive Boolean @default(true), createdAt, updatedAt) với unique index `(resourceCode, action, scope, departmentId, subDepartmentId, positionId, role)` và index `(resourceCode, action)` cho lookup; thêm CHECK constraint: `scope=DEPARTMENT → departmentId NOT NULL AND subDepartmentId IS NULL`, `scope=SUB_DEPARTMENT → subDepartmentId NOT NULL`, `scope=GLOBAL → cả hai NULL`. `positionLevelId` để phase 2. Chạy `npx prisma migrate dev` + `npx prisma generate`. Thêm backfill task 1.1b: audit và backfill `User.departmentId/subDepartmentId` + `Employee.positionId` cho dữ liệu hiện có; báo cáo số user thiếu mapping. ← (verify: `npx prisma validate` pass, `backend npx tsc --noEmit` 0 lỗi)
- [ ] 1.2 Seed `Resource` catalog từ scan thực tế và seed `Rule` rows từ quét `authorize(...)` hard-code (script idempotent, upsert theo unique key, chạy trong transaction, có dry-run mode in số Rule sẽ tạo, có báo cáo "uncovered routes" so với `grep -rn authorize backend/src/routes --include="*.ts" --exclude-dir=__tests__`). `action` suy từ HTTP method, `resource` suy từ ROUTE_MAP key — **không** seed `*/*`; ví dụ `DELETE /api/invoices/:id` + `authorize('ADMIN')` → `Rule(ADMIN, invoices, DELETE)`. Route không có authorize → không seed (baseline REQ-RBAC-006 sẽ cho phép). ← (verify: số Rule khớp số call sites authorize sau khi trừ tests/definition; spot-check 5 route tiêu biểu)
- [ ] 1.3 Mở rộng `UserSecondaryDepartment` thêm `positionId String?` (FK Position.id, nullable, SetNull on delete) kèm index; backfill từ `Employee.positionId` khi suy được. Tạo `RuleAuditLog` (`@@schema("auth")`, fields: id cuid, ruleId? FK Rule.id SetNull, actorId String?, action enum `CREATE|UPDATE|DELETE`, before Json? @db.JsonB, after Json? @db.JsonB, createdAt @default(now()), indexes `[ruleId, createdAt]`, `[createdAt]`) hoặc tái dùng `AuditLog` với `entityType='Rule'` — quyết định ở design D1 (chọn bảng riêng để query nhanh). ← (verify: migration không mất dữ liệu secondary departments hiện có)
- [ ] 1.4 Thêm mapping `Position.defaultRole` (field nullable `defaultRole UserRole?` trên `Position` hoặc bảng `PositionRoleMapping(positionId PK, defaultRole UserRole)`) và seed mapping mặc định cho 30+ Position hiện có (Trưởng phòng/bộ phận → DEPARTMENT_HEAD, Tổ trưởng → TEAM_LEAD, Nhân viên → EMPLOYEE). Khi `User.role` và `Position.defaultRole` khác nhau, Rule engine ưu tiên `Position.defaultRole` cho lookup, `User.role` giữ cho ADMIN bypass và baseline DELETE guard. ← (verify: mọi Position seed đều có defaultRole)

## 2. Backend — Rule CRUD và Audit

- [ ] 2.1 Implement `ruleService` với: `listRules(filters: { departmentId?, subDepartmentId?, positionId?, resourceCode?, action?, scope?})`, `getRuleById`, `createRule`, `updateRule`, `deleteRule` (soft-delete hoặc hard-delete kèm audit), `getMatrix({ positionId?, departmentId? })` trả về ma trận Position×Resource×Action với trạng thái `allow | deny | baseline-allow | baseline-deny | inherit | none` (baseline theo REQ-RBAC-006), và `getMyPermissions(userId)` resolve tập quyền của user (primary + secondary, theo thứ tự `Position > UserRole` và `SUB_DEPARTMENT > DEPARTMENT > GLOBAL`, áp dụng baseline khi không có Rule). Mỗi write SHALL ghi `RuleAuditLog` và gọi `invalidateRuleCache()`. Thêm validation: mỗi ô chỉ một Rule active (`409` nếu trùng), DELETE baseline guard không cần Rule explicit. ← (verify: service unit test cho inherit/override/deny-wins/baseline-DELETE/baseline-allow)
- [ ] 2.2 Implement `ruleController` và `ruleRoutes` (`/api/rules`, `/api/rules/:id`, `/api/rules/matrix`, `/api/rules/my-permissions`, `/api/rules/audit-log`, `/api/resources`), đăng ký vào `ROUTE_MAP` (`rule: '/api/rules'`, `resource: '/api/resources'`), bảo vệ bằng `authenticate` + `requireRule('rules', ...)` hoặc `authorize('ADMIN')` trong giai đoạn dual-enforcement. Shape response đúng chuẩn `{ success, data, pagination? }`. ← (verify: `ROUTE_MAP` có đủ keys mới, route xuất hiện trong server logs)
- [ ] 2.3 Implement `GET /api/resources` và `GET /api/rules/my-permissions` với shape chuẩn; `my-permissions` SHALL resolve từ Position + Department + SecondaryDepartments của user hiện tại, đã áp dụng baseline REQ-RBAC-006 (mọi action allow trừ DELETE, DELETE chỉ DEPARTMENT_HEAD/ADMIN) và `canDelete` flag cho frontend gating. ← (verify: gọi `my-permissions` với EMPLOYEE vs DEPARTMENT_HEAD cùng phòng ban cho kết quả DELETE khác nhau)

## 3. Backend — Enforcement DB-driven

- [ ] 3.1 Implement middleware `requireRule(resourceCode, action)` đọc Rule Matrix từ cache/DB, resolve theo thứ tự `Position > UserRole` và `SUB_DEPARTMENT > DEPARTMENT > GLOBAL` (PositionLevel phase 2), deny override allow ở cùng scope, scope hẹp thắng scope rộng, fallback về baseline REQ-RBAC-006 (allow mọi action trừ DELETE) khi không có Rule nào khớp, ADMIN bypass giữ nguyên có flag `adminStrictMode`. Thêm in-memory cache per-instance (Map + TTL 60s) keyed by `(resource, action)` và hàm `invalidateRuleCache()`. Thứ tự middleware trên route: `authenticate → requireRule(resource, action)`. ← (verify: unit test cho mọi thứ tự ưu tiên, deny-wins, scope-narrow-wins, baseline-DELETE-guard, ADMIN bypass)
- [ ] 3.2 Thay thế hard-code `authorize(...)` trong `backend/src/routes/*.ts` bằng `requireRule(resource, action)` theo Resource catalog — thực hiện theo batch để an toàn:
  - 3.2a: Script quét toàn bộ `authorize(...)` sinh danh sách mapping `route file + HTTP method + resource + action + roles` (dry-run, review).
  - 3.2b: Thay theo batch ~20 files/batch, mỗi batch 1 commit, giữ dual-enforcement (DB check + hard-code fallback + log divergence vào AuditLog với `{ ruleId, route, legacyAllow, dbAllow }`) trong 1 release.
  - 3.2c: Grep verify không còn `authorize(` ngoài `middlewares/auth.ts` và `ruleRoutes` (fallback); tắt fallback khi 7 ngày không còn divergence. Đối chiếu với danh sách 82 route files. ← (verify: `grep -rn "authorize(" backend/src/routes --include="*.ts" | wc -l` == 0 ngoài whitelist)
- [ ] 3.3 Hợp nhất `checkAccess({ checkDepartment, checkSubDepartment })` vào cùng pipeline với `requireRule` — ABAC suy từ `Rule.scope` thay vì truyền thủ công; `requireRule` tự gắn `req.userDepartmentIds / req.userSubDepartmentId` khi scope là DEPARTMENT/SUB_DEPARTMENT. Baseline REQ-RBAC-006 cũng là ABAC: user chỉ được thao tác resource thuộc phòng ban của mình (primary hoặc secondary), trừ khi có Rule GLOBAL cross-department. ← (verify: `employeeEvaluationRoutes` không còn cần `checkAccess` riêng; grep `checkAccess` còn lại == 0)
- [ ] 3.4 Thêm focused backend tests cho Rule CRUD, matrix (kèm baseline states), my-permissions (EMPLOYEE vs DEPARTMENT_HEAD DELETE), enforcement priority, secondary department, audit trail, route registration, và negative test `403` (không phải `500`) cho user thiếu quyền + ADMIN bypass. ← (verify: `backend npm test` pass, không skip)

## 4. Frontend — Đồng bộ Rule Matrix

- [ ] 4.1 Thêm frontend service types và API methods cho Rule/Resource (`frontend/src/services/ruleService.ts`) với: `listRules`, `getMatrix`, `getMyPermissions`, `listResources`, `createRule`, `updateRule`, `deleteRule`, `getRuleAuditLog`. Thêm TanStack Query hooks và structured query key factory (`ruleKeys: { all, lists, list(filters), detail(id), matrix(positionId, departmentId), myPermissions() }`, `resourceKeys`). ← (verify: components consume hooks chứ không gọi `apiClient` trực tiếp)
- [ ] 4.2 Refactor `frontend/src/utils/permissions.ts` để đọc từ `my-permissions` API (cache trong AuthContext/React Query), expose `can(resource, action)` và `canAccessDepartment(dept)` suy từ Rule Matrix + baseline REQ-RBAC-006; `can('invoices','DELETE')` SHALL trả về false cho EMPLOYEE/TEAM_LEAD và true cho DEPARTMENT_HEAD/ADMIN ngay cả khi chưa có Rule; giữ fallback hard-code `DEPARTMENTS/SUB_DEPARTMENTS` trong giai đoạn chuyển đổi. Cập nhật `AuthContext` để fetch `my-permissions` sau login và sau Rule change (invalidate query). ← (verify: sidebar gating đúng với Rule Matrix + baseline; nút Xóa ẩn cho EMPLOYEE)
- [ ] 4.3 Thêm trang quản trị Rule (Admin) — bảng ma trận Position × Resource × Action có filter theo Position/Department/Resource, CRUD Rule (thêm/sửa/xóa) với form chọn scope/position/resource/action/allow, hiển thị trạng thái `allow | deny | baseline-allow | baseline-deny | inherited | override` (baseline theo REQ-RBAC-006), và tab audit log (`RuleAuditLog` với CREATE/UPDATE/DELETE). UI tuân `openspec/ui-dna.md`: dense table, compact controls, pagination/filter gần bảng, copy tiếng Việt, bo góc vừa phải. ← (verify: admin tạo/sửa/xóa Rule và thấy hiệu lực ngay sau invalidate)
- [ ] 4.4 Cập nhật sidebar/menu/button gating toàn app để dùng `can(resource, action)` thay vì `DEPARTMENT_PERMISSIONS` hard-code; nút `DELETE` SHALL ẩn/disable cho EMPLOYEE/TEAM_LEAD theo baseline. Đảm bảo user chỉ thấy menu thuộc phạm vi Position/Department của mình (kể cả secondary). ← (verify: login với 3 Position khác nhau (EMPLOYEE, TEAM_LEAD, DEPARTMENT_HEAD) cùng phòng ban cho 3 mức DELETE khác nhau; secondary department user thấy quyền hợp nhất đúng)

## 5. Verification

- [ ] 5.1 Chạy `cd backend && npx tsc --noEmit` — phải pass 0 lỗi.
- [ ] 5.2 Chạy `cd backend && npm run lint` — phải pass.
- [ ] 5.3 Chạy `cd backend && npm test` — tất cả tests pass, không skip.
- [ ] 5.4 Chạy `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — phải pass 0 lỗi.
- [ ] 5.5 Chạy `cd frontend && npm run lint` — phải pass.
- [ ] 5.6 Kiểm tra thủ công:
  - Đăng nhập với 3 user cùng phòng ban nhưng khác role (EMPLOYEE, TEAM_LEAD, DEPARTMENT_HEAD): cả 3 đều GET/POST/PUT được; chỉ DEPARTMENT_HEAD DELETE được; EMPLOYEE/TEAM_LEAD DELETE nhận `403` (không phải `500`).
  - Đăng nhập với user `EMPLOYEE/Accounting` cố gọi `GET /api/invoices` (thuộc Accounting) → allow (baseline); cố gọi `GET /api/warehouse-receipts` (thuộc Production, không phải phòng ban của mình) → `403`.
  - ADMIN vẫn bypass toàn bộ.
  - Secondary department user (primary Production EMPLOYEE + secondary Warehouse DEPARTMENT_HEAD) → DELETE warehouseReceipts được qua secondary.
