## 1. Backend - pricing overview aggregation service

- [ ] 1.1 Create `backend/src/services/pricingOverviewService.ts` with helper `buildDateRange(month?, year?)` returning half-open `[gte, lt)` UTC range or undefined when both omitted; unit-test boundary at 00:00+07 vs 17:00Z. Share or mirror the existing `customerType` predicate so `byCustomerType` matches list endpoints (`InternationalCustomer.quocGia` presence → QuocTe) and the purchase pending normalization (trim/lower/diacritic-strip, "chờ duyệt" | "cho duyet" | "cho_duyet" → pending).
- [ ] 1.2 Implement aggregation helpers via Prisma `groupBy`/`count`/`aggregate` running in `Promise.all` (no N+1): `requests` (`status` groupBy zero-filled over 4 `QuotationRequestStatus`, `byCustomerType`, `total`), `quotations` (9 `QuotationStatus` groupBy zero-filled, `priceLockedCount`, `byCustomerType`), `orders` (`production` 7 statuses + `payment` 3 statuses groupBys + `totalValueVND` via `_sum.giaTriDonHangVND` null→0), `costs` (`generalTotal`/`exportTotal` counts, `_avg.giaThanhNgay` over both tables ignoring nulls, top-2 `loaiChiPhi` merged by `_sum.giaThanhNgay` desc), `approvals` (`OvertimePlan.CHO_DUYET` count + purchase pending count), `warnings` (non-terminal `daysOpen >=7/<14` yellow vs `>=14` red). Respect D2: date window scopes requests/quotations/orders/costs; approvals/warnings are backlog (un-windowed).
- [ ] 1.3 Add `backend/src/constants/quotationAging.ts` (or reuse existing) exporting `AGING_THRESHOLD=7`, `AGING_RED=14`, `NON_TERMINAL_STATUSES` matching `QuotationManagement.tsx:23-26`; both frontend and backend import one truth (fallback: duplicate with equality test). Add `getOverview(month?, year?)` returning typed `PricingOverview` DTO matching proposal shape.

## 2. Backend - controller + route + ROUTE_MAP

- [ ] 2.1 Create `backend/src/controllers/pricingOverviewController.ts` validating `month` (1-12) and `year` (4-digit) via zod, 400 on malformed, calling `pricingOverviewService.getOverview`, returning standard `{ success: true, data }` envelope. Map service errors to `ValidationError`/`AppError` via `@utils/errors`, not `res.status(500)`.
- [ ] 2.2 Create `backend/src/routes/pricingOverviewRoutes.ts` with `GET /` behind `authenticate` + `isPricingMember` gate mirroring `pricing-room-review-tabs` helper (`ADMIN` bypass, else GENERAL `DEPARTMENT_HEAD`/`TEAM_LEAD` or GENERAL/pricing `EMPLOYEE` including `secondaryDepartments`). Reuse or extract `isPricingApprover`/`isPricingMember` from other pricing services; 403 for non-members. No `PATCH /status` endpoint.
- [ ] 2.3 Register route in `backend/src/routes/index.ts` `ROUTE_MAP` as `pricingOverview: '/api/pricing/overview'` so `registerRoutes` auto-discovers the file. Verify server logs show `Registered ... API routes` includes the new path. ← (verify: `GET /api/pricing/overview` 200 for pricing member, 403 for non-pricing, 400 for `?month=13`)

## 3. Frontend - service + hook + types

- [ ] 3.1 Create `frontend/src/services/pricingOverviewService.ts` with `getOverview(month?: number, year?: number): Promise<PricingOverview>` using `apiClient` (`@services` alias if applicable), typed DTO `{ requests, quotations, orders, costs, approvals, warnings }` as in proposal; normalize nulls (`totalValueVND` null→0, `avgGiaThanhNgay` nullable). Vietnamese error messages on throw.
- [ ] 3.2 Create `frontend/src/hooks/usePricingOverview.ts` wrapping TanStack Query with key factory `{ all: ['pricingOverview'], overview: (m,y) => [...all, 'overview', m, y] }`, `staleTime` ~60s, `refetchOnWindowFocus: false`. Invalidate via `queryClient.invalidateQueries({ queryKey: pricingOverviewKeys.all })` if any mutation touches pricing domain (optional). ← (verify: switching month/year triggers exactly one network request)
- [ ] 3.3 Optionally extract shared type `frontend/src/types/pricingOverview.ts` re-exported by service/hook; keep Vietnamese labels in components, not in DTO.

## 4. Frontend - GeneralPricing 5-card 2-row dashboard

- [ ] 4.1 Update `frontend/src/pages/general/GeneralPricing.tsx`: remove `fetchAllStats`/`fetchPendingCounts`, 9 parallel `getAll(1,1)` calls, badge count state, and the purchase 1-1000 client filter; replace with single `usePricingOverview(selectedMonth, selectedYear)` call. Keep `selectedMonth`/`selectedYear` state and selects, `VALID_TABS` and 6 tabs untouched, tab `?tab=` deep-link logic unchanged. Wire `isLoading` → per-card skeletons, `isError` → inline Vietnamese retry (`"Không tải được tổng quan. Thử lại."`).
- [ ] 4.2 Render Row 1 as `grid grid-cols-1 lg:grid-cols-3 gap-6`: Card YCBG (total + 4 status pills `CHO_XU_LY/DANG_BAO_GIA/DA_BAO_GIA/HUY` + QuocTe/NoiDia), Card BaoGia (total + 5 grouped statuses per D6 + `priceLocked` lock badge), Card DonHang (total + `totalValueVND` `vi-VN` VND + production/payment breakdown). Cards use `openspec/ui-dna.md` tokens: white surface, `border` not heavy shadow, `rounded-xl`, compact 8px rhythm, blue primary for totals, green/red only semantically. Each card `onClick → setActiveTab('requests'/'quotes'/'orders')` preserved.
- [ ] 4.3 Render Row 2 as `grid grid-cols-1 lg:grid-cols-3 gap-6`: Card ChiPhi (`lg:col-span-2`, general+export totals + `avgGiaThanhNgay` with `—` when null + top-2 `loaiChiPhi` chips, omitted when empty) and Card ChoDuyet&CanhBao (`lg:col-span-1`, `overtimePending` + `purchasePending` with red badge when >0, `agingYellow`/`agingRed` counts, footer `"Tồn chờ xử lý — Không áp dụng bộ lọc tháng/năm"` with tooltip). `ChiPhi` → `setActiveTab('costs')`, `ChoDuyet&CanhBao` splits click area to `overtime-review`/`purchase-review`. Stack to single column below `lg`. No new dependency, no chart lib.
- [ ] 4.4 Vietnamese copy pass + noise rules: hide zero-count breakdown rows, format `totalValueVND` via `toLocaleString('vi-VN')`, keep concise operational labels per D9; confirm no raw IDs/status codes without human labels. ← (verify: month/year toggle refreshes all 5 cards from one response; badges no longer fire separate requests; cards still switch tabs; empty datasets show `—` not placeholder noise)

## 5. Verification

- [ ] 5.1 `cd backend && npx tsc --noEmit` — zero errors ← (verify: no TS errors)
- [ ] 5.2 `cd frontend && npx tsc --noEmit -p tsconfig.app.json` — zero errors, no `TS2304` ← (verify: no TS errors)
- [ ] 5.3 `cd backend && npm run lint` and `cd frontend && npm run lint` — no new violations
- [ ] 5.4 `cd backend && npm test -- --runInBand` — existing suites green + new `pricingOverviewService`/`pricingOverviewController` tests for filter semantics, zero-fill, customerType/p pending normalization, aging bands, and 403/400 guards ← (verify: all tests pass)
- [ ] 5.5 Manual on dev DB: login as `ADMIN` and as `GENERAL/pricing EMPLOYEE` — verify 5-card dashboard renders, month/year filter scopes requests/quotations/orders/costs but not backlog warnings, totals match direct `groupBy` spot-checks, non-pricing user gets 403 on `GET /api/pricing/overview`
