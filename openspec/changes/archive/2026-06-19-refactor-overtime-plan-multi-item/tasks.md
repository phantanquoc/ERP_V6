## 1. Prisma schema and migration

- [x] 1.1 Capture a `pg_dump` of the `common` schema in dev and document the rollback command in the change directory before touching the schema
- [x] 1.2 Edit `backend/prisma/schema/common.prisma`: add the new `OvertimePlanItem` model with `id` (cuid), `overtimePlanId` (FK with `onDelete: Cascade`), `ngayTangCa: DateTime`, `gioBatDau: String`, `gioKetThuc: String`, `workShiftId: String?` (FK with `onDelete: SetNull`), `workShiftName: String?`, `nguoiThamGiaIds: String[]`, `ghiChuItem: String?`, `trangThaiTiepNhan: Json @default("{}")`, `gioThucTe: Json @default("{}")`, `createdAt`, `updatedAt`, and `@@map("overtime_plan_items")` plus indexes on `[overtimePlanId]`, `[ngayTangCa]`, `[workShiftId]`, plus `@@schema("common")`
- [x] 1.3 Add the back-relation `items OvertimePlanItem[]` to `OvertimePlan`, add `overtimePlans OvertimePlanItem[]` back-relation on `WorkShift` if it does not already exist, and remove the six fields from `OvertimePlan`: `ngayTangCa`, `gioBatDau`, `gioKetThuc`, `nguoiThamGiaIds`, `trangThaiTiepNhan`, `gioThucTe`
- [x] 1.4 Generate the migration with `npx prisma migrate dev --name overtime_plan_multi_item --create-only`; rename the directory so the timestamp is strictly greater than `20260618000000_refactor_machine_as_physical_instance` (e.g., `20260619HHMMSS_overtime_plan_multi_item`)
- [x] 1.5 Hand-edit the generated SQL so that, in one transaction: (a) `CREATE TABLE common.overtime_plan_items` with FKs and indexes, (b) `INSERT INTO common.overtime_plan_items (id, overtimePlanId, ngayTangCa, gioBatDau, gioKetThuc, nguoiThamGiaIds, trangThaiTiepNhan, gioThucTe, createdAt, updatedAt) SELECT gen_random_uuid()::text, id, ngayTangCa, gioBatDau, gioKetThuc, nguoiThamGiaIds, COALESCE(trangThaiTiepNhan, '{}'::jsonb), COALESCE(gioThucTe, '{}'::jsonb), createdAt, updatedAt FROM common.overtime_plans` (cuid replacement is acceptable since Prisma generates ids application-side; alternatively use `concat('ovt_item_', gen_random_uuid())`), (c) `ALTER TABLE common.overtime_plans DROP COLUMN ngayTangCa, DROP COLUMN gioBatDau, DROP COLUMN gioKetThuc, DROP COLUMN nguoiThamGiaIds, DROP COLUMN trangThaiTiepNhan, DROP COLUMN gioThucTe`
- [x] 1.6 Run `npx prisma migrate dev` and verify: `SELECT COUNT(*) FROM common.overtime_plan_items` equals the original `SELECT COUNT(*) FROM common.overtime_plans`; spot-check that two existing rows preserve their `trangThaiTiepNhan` and `gioThucTe` content; verify the six dropped columns no longer exist on `overtime_plans` ← (verify: schema matches design.md decisions 1, 3, 5; migration runs in one transaction; backfilled row count matches; spec scenarios "Backfill row count", "JSON maps preserved", and "Source columns dropped after backfill" all hold)

## 2. Backend types, service, controller, routes

- [x] 2.1 Update `backend/src/types/overtimePlan.types.ts`: add `OvertimePlanItemInput { ngayTangCa: string; gioBatDau: string; gioKetThuc: string; workShiftId?: string; nguoiThamGia: string[]; ghiChuItem?: string }`; replace the flat fields on `CreateOvertimePlanRequest` with `items: OvertimePlanItemInput[]`; update `UpdateOvertimePlanRequest` analogously; export a populated `OvertimePlanItem` type with `nguoiThamGia: User[]` for response shape
- [x] 2.2 In `backend/src/services/overtimePlanService.ts`, rewrite `create(data, userId, files)` to wrap the parent insert and `createMany` for items in `prisma.$transaction`; per item, validate `gioKetThuc > gioBatDau`, validate every `employeeId` in `nguoiThamGia` resolves to an active user (mapping to `userId`), look up the `WorkShift` to snapshot `workShiftName` when `workShiftId` is provided, and reject calls where `items` is empty
- [x] 2.3 Change the `update` signature to `update(id, data, userId, isAdmin, files)`; bypass both the ownership check (`plan.nguoiTaoId !== userId`) AND the pending-only check (`plan.trangThai !== 'CHO_DUYET'`) when `isAdmin` is true; otherwise keep both checks; update items using delete-then-recreate inside `prisma.$transaction`
- [x] 2.4 Change the `delete` signature to `delete(id, userId, isAdmin)`; bypass the ownership check when `isAdmin` is true; cascade is handled by the FK
- [x] 2.5 Rewrite `approvePlan` so that on `DA_DUYET` transition, the service iterates `plan.items × item.nguoiThamGiaIds` and creates one `Attendance` row per `(employeeId, item.ngayTangCa)` with `checkInTime = item.gioBatDau`, `checkOutTime = item.gioKetThuc`, `isOvertime = true`, skipping triples that already exist; wrap the entire materialization in `prisma.$transaction`
- [x] 2.6 Change `acceptPlan` to accept an `itemId` argument and operate on `OvertimePlanItem.trangThaiTiepNhan` for that specific item; change `updateActualTime` similarly to operate on `OvertimePlanItem.gioThucTe`; both must verify the user is in the item's `nguoiThamGiaIds`
- [x] 2.7 Update `populateWithUsers` and `batchPopulateWithUsers` to include `items` and populate each item's `nguoiThamGia` from `nguoiThamGiaIds`; preserve item ordering by `ngayTangCa` ASC
- [x] 2.8 Update `backend/src/controllers/overtimePlanController.ts`: in `create` and `update`, parse `req.body.items` with `JSON.parse` (FormData carries it as a string); pass `isAdmin = req.user?.role === 'ADMIN'` to `update` and `delete`; in `acceptPlan` and `updateActualTime`, read `itemId` from `req.body` and forward to the service
- [x] 2.9 Confirm `backend/src/routes/overtimePlanRoutes.ts` requires no path changes (the controller absorbs the new shape); spot-check ROUTE_MAP in `backend/src/routes/index.ts` still lists `/api/overtime-plans` ← (verify: spec scenarios "Plan with multiple distinct days", "Cascade delete", "Snapshot on create", "Multi-day approval fans out", "Idempotent re-approval", "Admin edits another user's pending plan", "Admin edits an approved plan", "Non-admin still blocked", "Per-item acceptance", "Per-item actual time", "Round-trip" all hold; service tests pass)

## 3. Frontend hooks, services, modals

- [x] 3.1 Create `frontend/src/hooks/useWorkShifts.ts`: TanStack Query hook fetching `/api/work-shifts`, filtering to `isActive` rows, `staleTime: 5 * 60 * 1000`; export the standard query-key factory shape
- [x] 3.2 Update `frontend/src/services/overtimePlanService.ts`: introduce `OvertimePlanItem` (with `nguoiThamGia: User[]` populated) and `OvertimePlanItemInput` types; replace flat date/time/participant fields on `OvertimePlan` and `CreateOvertimePlanData` with `items: OvertimePlanItem[]` / `items: OvertimePlanItemInput[]`; update `buildFormData` to append `items` as `JSON.stringify(items)`
- [x] 3.3 Add `useUpdateOvertimePlan` mutation to `frontend/src/hooks/useOvertimePlans.ts`, mirroring `useCreateOvertimePlan`'s shape and invalidating the same query keys; verify `useDeleteOvertimePlan` is exported from the same file
- [x] 3.4 Rewrite `frontend/src/components/CreateOvertimePlanModal.tsx`: keep the existing header (Nội dung, Mức độ ưu tiên, Ghi chú, FileUpload); add an items table with columns Ngày | Ca làm việc | Nhân sự | Giờ bắt đầu | Giờ kết thúc | Tổng giờ | row delete; selecting a `WorkShift` autofills `gioBatDau`/`gioKetThuc` while keeping `workShiftId`; add "Thêm dòng" with defaults today/no-shift/empty/17:00-19:00; render footer with total slot hours and total man-hours; reject submission unless `items.length >= 1` and every row has a date, both times with end > start, and at least one participant
- [x] 3.5 Wire edit mode in the modal: when `initialData` is provided (admin Pencil action), preload `items` and `files`; on submit call `useUpdateOvertimePlan`
- [x] 3.6 Update `frontend/src/components/OvertimePlanListModal.tsx` list view: render the "Ngày tăng ca" column as `1 ngày DD/MM` for single-item plans and `N ngày (DD/MM – DD/MM)` for multi-item plans using earliest/latest `ngayTangCa`; add Pencil and Trash icon buttons in the action column for `user.role === 'ADMIN'` on every row regardless of `trangThai`; Trash shows a confirm dialog before invoking `useDeleteOvertimePlan`; Pencil opens the create/edit modal with `initialData = plan`
- [x] 3.7 Update the detail modal section of `OvertimePlanListModal.tsx`: replace the flat date/time block with an items sub-table (Ngày | Ca | Nhân sự | Giờ bắt đầu | Giờ kết thúc | Tổng giờ), one row per item, ascending by `ngayTangCa`
- [x] 3.8 Replace the file rendering block (lines ~309-321) with a card per file: pick icon by extension (FileImage for `jpg|jpeg|png|gif|webp`, FileSpreadsheet for `xlsx|xls|csv`, FileCode for `doc|docx`, FileText otherwise); display friendly name (basename with leading `^\d+-` prefix stripped); for image extensions, render an 80×80 `<img src={getFileUrl(file)}>` thumbnail with `object-fit: cover` before the icon; the entire card is an `<a target="_blank" rel="noopener noreferrer">` to `getFileUrl(file)` ← (verify: spec scenarios "Add row with defaults", "Footer recomputes on edit", "Submit blocked when row is incomplete", "Submit blocked when end time is before start time", "Single-item plan", "Multi-item plan", "Detail enumeration", "Admin sees actions on every row", "Admin deletes after confirm", "Non-admin restricted", "PDF file", "Image file thumbnail", "Spreadsheet file", "Autofill on selection", "Override after autofill" all hold by exercising the modal in dev)

## 4. AI service and tests

- [x] 4.1 In `ai-service/agent/registry.py`, if a `create_overtime_plan` tool already exists, change its parameter schema to accept `items: List[Dict]` (each: `ngayTangCa`, `gioBatDau`, `gioKetThuc`, optional `workShiftId`, `nguoiThamGia: List[str]`, optional `ghiChuItem`) instead of the flat date/time/participant fields; if the tool does not exist, do nothing here
- [x] 4.2 Update `ai-service/tests/test_registry.py` total tool count only if step 4.1 added or removed a tool (current count is 66; net delta is expected to be 0 unless the registry already had a now-incompatible flat-shape tool that needs renaming)
- [x] 4.3 Inspect `backend/src/__tests__/payrollKpiDeduction.test.ts` (and any other test that fixtures overtime data); update fixtures to create `OvertimePlanItem` rows where the test previously inserted flat overtime fields ← (verify: `cd ai-service && python3 -m pytest tests/test_registry.py -x -q` passes; backend Jest suites that touch overtime continue to pass)

## 5. Verification, smoke tests, rollout

- [x] 5.1 Run `cd backend && npx tsc --noEmit && npm run lint && npm test` and resolve every error
- [x] 5.2 Run `cd frontend && npx tsc --noEmit && npm run lint` and resolve every error
- [x] 5.3 Run `cd ai-service && python3 -m pytest tests/ -x -q` and resolve every failure
- [x] 5.4 Smoke test create flow: open the form, add three rows (different dates, different shifts, different rosters), confirm autofill on shift selection, confirm footer recomputes, submit, verify the list shows `3 ngày (...)` and the detail modal shows three sub-rows
- [x] 5.5 Smoke test admin edit/delete: as admin, edit a `DA_DUYET` plan created by another user (change one item's hours), then delete a third user's plan after confirming
- [x] 5.6 Smoke test approval fan-out: approve a 2-item plan with overlapping participants and verify `Attendance` rows materialized one per `(employeeId, item.ngayTangCa)` with the right times
- [x] 5.7 Smoke test file preview: attach one PDF, one PNG, one XLSX; verify card icons match the type, the filename has no timestamp prefix, the PNG card shows a thumbnail, and clicking each opens in a new tab
- [x] 5.8 Document the rollback procedure (restore the `common` schema from the dump captured in 1.1) in the change directory and add release notes flagging the per-item `acceptPlan` / `updateActualTime` payload change ← (verify: every spec scenario in `specs/overtime-plan-multi-item/spec.md` has a matching observable behavior in the dev environment, and the Migration Plan in `design.md` has been executed end to end)
