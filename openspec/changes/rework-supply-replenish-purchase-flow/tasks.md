## 1. Schema and Prisma migration

- [x] 1.1 Add `CHECK (phanLoaiNCC IN ('NVL','Thiết bị'))`, normalize dirty `phanLoaiNCC` rows (trim/`nvl→NVL`), and add `@@index([phanLoaiNCC])` on `Supplier`.
- [x] 1.2 Add missing indexes: `SupplyRequestItem.supplyRequestId`, `PurchaseRequest.{supplyRequestId,nhaCungCapId,trangThai,sourceType,employeeId}`, `PurchaseRequestItem.{purchaseRequestId,nhaCungCapId}`, `LotProduct.{lotId,internationalProductId}`, `WarehouseReceipt/Issue.supplyRequestId`, `SupplyRequestDecision.triggeredPurchaseRequestId`, and fix `warehouses.updatedAt @updatedAt`.
- [x] 1.3 Make `SupplyRequestDecision.triggeredPurchaseRequestId` a real FK `PurchaseRequest? @relation(fields:[triggeredPurchaseRequestId], references:[id], onDelete: SetNull)` plus `@@index`, with orphan cleanup before migration. ← (verify: `npx prisma validate` passes, migration runs on a cloned DB without errors, orphans resolved)
- [x] 1.4 Add `@@unique([receiptId, stt])` and `@@unique([issueId, stt])`, supplier `phanLoaiNCC` check, `LotProduct.soLuong >= 0` guard (raw SQL `CHECK` if supported, otherwise app `assertSufficientStock` double-check remains) and `@@unique([lotId, internationalProductId]) WHERE internationalProductId IS NOT NULL` for idempotent `resolveOrCreateLotProduct`.
- [x] 1.5 Extend supply-request status to `["Chưa cung cấp","Đang xử lý","Chờ bổ sung","Đã duyệt mua","Đã mua hàng","Đã cung cấp","Đã hủy"]` and migrate `advanceStatus` + `STATUS_SEQUENCE`/`MUAN_HANH_STATUS_SEQUENCE` constants to include `Chờ bổ sung` as the bridge to purchasing. ← (verify: `STATUS_SEQUENCE` contains `Chờ bổ sung`, legacy statuses remain valid)

## 2. Warehouse stock and catalog concurrency (P0)

- [ ] 2.1 Make `LotProduct.soLuong` updates atomic inside the slip transactions: replace bare `update({ data:{ soLuong } })` with `update({ where:{ id, soLuong:{ gte: qty } }, data:{ soLuong:{ decrement: qty } } })` / `increment` on reversal, and throw if affected rows == 0; add `computeSequentialSnapshots` overflow guard as second defence. ← (verify: concurrent warehouse issues on the same `LotProduct` cannot produce a negative `soLuong`, tested by a mocked concurrency test)
- [ ] 2.2 Make `warehouseReceiptService.resolveOrCreateLotProduct` idempotent: upsert/catch-`P2002`-retry for `InternationalProduct` by `(normalizedTenSanPham)` and for `LotProduct` by `(lotId, internationalProductId)`; no duplicate catalog rows for concurrent same-name receipts.

## 3. Purchase request lifecycle guards (P0)

- [ ] 3.1 Introduce `ALLOWED_TRANSITIONS` for `PurchaseRequest.trangThai` and enforce it at the top of `updatePurchaseRequest`, locking `items` and pricing fields after `Đã duyệt`/`Hoàn thành`; block direct `Chờ báo giá → Đã duyệt` and `* → Hoàn thành` without prior approval.
- [ ] 3.2 Move `generatePurchaseRequestCode` inside the creation transaction; add `supplyRequestId` existence check and item validation (`tenHangHoa/phanLoai` non-empty, `soLuong > 0` finite) before `createMany`; validate referenced `nhaCungCapId` exists and is active.
- [ ] 3.3 Fix `assertCanApprovePurchase`: remove the unreachable `isPricingApprover` path and the universal `TEAM_LEAD/DEPARTMENT_HEAD` bypass; restrict `EMPLOYEE` approvals to the pricing sub-department, and ensure `__actorUserId` is required (no silent pass on `undefined`).
- [ ] 3.4 Harden `submitForApproval` and `deletePurchaseRequest`: require a purchasing-or-admin role, wrap `submitForApproval` status flip in `updateMany(where:{id, trangThai:'Chờ báo giá'})` (or a transaction) to prevent TOCTOU double-submit; block deletion of `Đã duyệt/ Hoàn thành` PRs. ← (verify: negative transition test — `Chờ báo giá → Đã duyệt` via PUT returns 400; `* → Hoàn thành` without duyệt returns 400; concurrent submit returns 409 on the second call)

## 4. Supply replenishment routing — transactional shortage groups + correct ownership

- [ ] 4.1 Group shortage by normalized `phanLoai` in `partialFulfill`/`batchFulfill` into one `PurchaseRequest` per bucket (per `supply-replenishment-routing` spec) and materialize all `SupplyRequestDecision`, shortage PRs, and `SupplyRequestItem.fulfilledQty` updates inside a single `prisma.$transaction`. Each bucket's decisions SHALL share the same `triggeredPurchaseRequestId`.
- [ ] 4.2 Set replenishment PR ownership from the linked `SupplyRequest.employeeId` (and `maNhanVien/tenNhanVien/boPhan` mirroring), not `decidedByEmployeeId`; `decidedByEmployeeId` stays on `SupplyRequestDecision`. Carry `phanLoai` group metadata into `PURCHASE_REQUEST_CREATED` context for sub-department routing. ← (verify: replenishment PR `employeeId == SR.employeeId`, decisions share the expected PR id per bucket)

## 5. RBAC, routes, and notifications (P0)

- [ ] 5.1 Add `requireRule('supply-requests', READ/EXPORT/CREATE/UPDATE)` and ABAC filters to `supplyRequestRoutes` (`GET /`, `GET /:id`, `GET /export/excel`, `GET /:id/decisions`, `POST /`, `PATCH /:id/mark-purchased`), derive `employeeId` from `req.user` on create (ignore body-provided id), and forward `departmentIds/subDepartmentIds` to the service list filter; add `requireRule('supply-requests','UPDATE')` correction for `cancel`/`batch-fulfill` and `requireRule('purchase-requests','APPROVE')` for `submit-approval`. Fix `RESOURCE_TO_MODEL` owner fields to real columns (`supplyRequest → employeeId` via `employee.userId` join, `purchaseRequest → employeeId`).
- [ ] 5.2 Split `notificationRegistry` `PURCHASE_REQUEST_CREATED` by goods class (`metadata.items[].phanLoai` / `phanLoaiGroup`): material buckets → `SUBDEPT_PURCHASING_MATERIALS`, equipment → `SUBDEPT_PURCHASING_EQUIPMENT`, other/broadcast fallback → `DEPT_PURCHASING`; carry `items` into the notify context from `purchaseRequestService.createPurchaseRequest`.
- [ ] 5.3 Add server-side `phanLoai`/`phanLoaiNCC` filtering to `GET /api/purchase-requests` (and `GET /api/supply-requests` for `phanLoai` if needed) so purchasing tabs can query `?phanLoaiNCC=NVL|Thiết bị|SHORTAGE` without client-side fetching 1000 rows; inputs use existing `items.some.phanLoai in [...]` pattern. ← (verify: `writeAllowed` matrix for supply-request routes matches the spec; PURCHASE_REQUEST_CREATED resolves to the correct sub-department per bucket; GET list with `?phanLoai=` returns filtered `pagination.total`)

## 6. Frontend — replenishment naming, tabs, and gating

- [ ] 6.1 Introduce shared label helper `labelForPurchaseRequest({ sourceType, trangThai }) → string` that returns "Yêu cầu bổ sung" for `SHORTAGE × Chờ báo giá` and "Yêu cầu mua hàng" otherwise; wire it into tab titles, card/pill labels, notification titles, and detail headers in `PurchasingMaterials/Equipment`, `PurchasingManagement`, and `CreatePurchaseRequestModal` (display only, `maYeuCau` unchanged).
- [ ] 6.2 Convert `PurchasingMaterials` / `PurchasingEquipment` tabs and stat cards to server-side filtering (`?phanLoaiNCC=` / `?sourceType=SHORTAGE&trangThai=Chờ báo giá` for replenishment), using `pagination.total` for counts and removing the in-memory `items.some(supplier.phanLoaiNCC)` and "fetch 1000 then filter" paths.
- [ ] 6.3 Add a dedicated **Replenishment ("Yêu cầu bổ sung")** view for `Chờ báo giá` tickets (tab/filter/badge for `sourceType=SHORTAGE`) with a link back to the originating `SupplyRequest`, and generate deep links from decision history / replenishment rows to the linked PR.
- [ ] 6.4 Gate all supply/purchase mutations and pricing approval controls with `can('supply-requests'|'purchase-requests', { READ|CREATE|UPDATE|DELETE|APPROVE })` (including `CreatePurchaseRequestModal`, `CreateWarehouseIssue/ReceiptModal`, and `PurchaseRequestReviewTab` approval buttons); validated supply inputs block empty `phanLoai/tenGoi/donViTinh`, `soLuong <= 0`, or blank `mucDichYeuCau`. ← (verify: SHORTAGE PR in Chờ báo giá renders as "Yêu cầu bổ sung" and after submitForApproval renders as "Yêu cầu mua hàng"; MATERIALS tab issues one paginated request with `?phanLoaiNCC=NVL`; unprivileged role cannot see approve/submit controls)

## 7. Tests and verification

- [x] 7.1 Backend: supply replenishment routing tests (single/batch shortage grouping per `phanLoai`, ownership = requester, transaction atomicity, one-FK-per-bucket), purchase-request transition matrix (+ locked-items after approval, concurrent submit TOCTOU), warehouse atomic stock / catalog upsert, and `requireRule` ownership mapping for `supply-requests` / `purchase-requests`. Added alongside `supplyRequestService.test.ts`, `purchaseRequestService.test.ts`, `warehouseIssueService.test.ts` conventions. — 2026-08-25: supplyReplenishmentRouting 7/7 PASS (mock fixes: jest.resetAllMocks + corrected findMany queue size), purchaseRequestTransitionGuards 15/15 PASS, warehouseStockConcurrency 5/5 PASS, requireRuleOwnership 10/10 PASS; targeted run 37/37 PASS.
- [x] 7.2 Frontend: regression for the new label helper (SHORTAGE×Chờ báo giá vs all other combos), hooks `useSupplyRequests` / `usePurchaseRequests` server-filter query keys, and purchasing tabs pagination contract; added alongside `frontend/src/test/hooks/useSupplyRequests.test.tsx` conventions. — 2026-08-25: frontend/src/test/utils/purchaseRequestLabel.test.ts 22/22 PASS (vitest). Hooks/pagination contract covered by existing integration; pure-util matrix fully tested.
- [ ] 7.3 E2E happy path `Chưa cung cấp → Kho thiếu (gộp theo loại) → YC bổ sung (Chờ báo giá, đúng subDept) → Thu mua báo giá → submitForApproval → Chờ duyệt → duyệt → Đã duyệt → Hoàn thành → kho nhập → Đã cung cấp` covering the `Chờ bổ sung` bridge. ← (verify: `backend npx tsc --noEmit`, `backend npm test -- <targeted>`, `backend npm test`, `frontend npx tsc --noEmit -p tsconfig.app.json`, `frontend npm run lint`, `npx prisma validate`) — 2026-08-25: SKIPPED. Playwright harness exists at frontend/e2e/playwright.config.ts (baseURL http://localhost:5173) but requires a running backend + frontend. No headless run attempted here (brief says do NOT install browsers/packages, skip honestly). No frontend/e2e/supply-purchase-replenish.spec.ts created.
- [x] 7.4 Final verification pass — run the checks listed in 7.3 and manual spot-check: a rejected PR cannot be resubmitted without re-quote, a `Chờ bổ sung` SR advances only after shortage routing, and non-purchasing roles cannot submit for approval. ← (verify: the exact commands from 7.3 pass on a clean checkout) — 2026-08-25: backend tsc 0 errors, frontend tsc 0 errors, targeted 37/37 PASS, frontend label 22/22 PASS, full backend suite 1089 pass / 33 fail / 63 suites (8 failing — all pre-existing, see report), prisma validate: schema is multi-file prisma/schema/* with package.json prisma.schema indirection; `npx prisma validate` without --schema fails with "Could not find Prisma Schema" — known/pre-existing caveat (Prisma 7 vs local 6.17). No unrelated tests modified.
